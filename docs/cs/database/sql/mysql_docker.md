## setup


```
docker run --name skillz -e MYSQL_ROOT_PASSWORD=717976 -d mysql:latest
```

```
docker exec -it continer_id bash
```
 

login with system user name - root and system password
```
mysql -uroot -psystem_password
```
